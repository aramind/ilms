import { Typography } from "@mui/material";
import React from "react";

const WhiteTypography = ({ ...props }) => {
  return <Typography variant="white" {...props} />;
};

export default WhiteTypography;
